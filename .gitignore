# Ignore Python compiled files
__pycache__/
*.py[cod]

# Ignore all files and directories under 'messages'
messages/

TSVZ.py
test.tsv
mainIndex.tsv

# Private-board runtime state (per-board indexes/files, registry, cookie key)
boards/
boards.nsv
.wpaste_secret

# Local runtime config (keep wpaste.config.example.json tracked)
wpaste.config.json

venv